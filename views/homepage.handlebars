{{#each posts as |post| }}
<div class="row mb-4 project">
  <div class="col-md-5">
    <h2>
      <a href="/post/{{id}}">{{title}}</a>
      <p>{{content}}</p>
    </h2>
    <p>Created by {{user.username}} on {{format_date}}</p>
  </div>
</div>
{{/each}}

