{{#each posts as |post| }}
  <div class="post-list">
    <h2 id="homepage-header">
      <a href="/post/{{id}}">{{title}}</a> 
      <p>Created by {{user.username}} on {{format_date}}</p>
    </h2>
    <p>{{content}}</p>
  </div>
{{/each}}

