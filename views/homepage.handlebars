{{#each posts as |post| }}
  <div class="post-list post-boxes">
    <h2 class="homepage-header">
      <a href="/post/{{id}}">{{title}}</a> 
      <p>Created by {{user.username}} on {{format_date createdAt}}</p>
    </h2>
    <p id="content-bg">{{content}}</p>
  </div>
{{/each}}

